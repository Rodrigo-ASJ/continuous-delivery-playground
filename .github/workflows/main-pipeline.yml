name: Main Pipeline
on: 
 push:
  branches:
  - "main"

jobs:
  Unittest:
  runs-on: ubuntu-latest
  steps:
    - name: "Lanzar Test unitarios"
      run: |
        set -ev 
        cd modern-web-app
        npm i
        npm run test:unit

      
